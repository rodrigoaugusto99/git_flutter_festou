import 'package:flutter/material.dart';

class Impostos extends StatefulWidget {
  const Impostos({super.key});

  @override
  State<Impostos> createState() => _ImpostosState();
}

class _ImpostosState extends State<Impostos> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
